rules:
  - id: sift.session-cookie-no-httponly
    message: |
      [Session Security] Cookie 未設定 httpOnly（XSS 可竊取 Token）。
    severity: WARNING
    languages:
      - python
      - javascript
      - typescript
      - go
      - java
      - php
    patterns:
      - pattern-regex: (res\.cookie\(|set_cookie\(|SetCookie\()
    metadata:
      category: security
      cwe: "CWE-614"
